export const metadata = {
  title: "My scanner stats",
  description: "Signed-in scanner operator profile and stats",
}

export default function RecScannerMeLayout({ children }) {
  return children
}
